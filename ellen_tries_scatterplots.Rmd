Scatterplots with Regression Lines


```{r}
# Load data
eardatalink = "https://raw.githubusercontent.com/rslav/finalprojectSLAM/master/eardata.csv"
eardata<- read.csv(eardatalink)

# View the first 5 rows
head(eardata, n = 5)

```

```{r}
library(ggplot2)
```

Justice (y) by Pregnancy Health (x)
```{r}
# Basic scatter plot
ggplot(eardata, aes(x=PHI, y=JusticeIndex)) + geom_point()

# Add the regression line
ggplot(eardata, aes(x=PHI, y=JusticeIndex)) + geom_point()+
  geom_smooth(method=lm) + labs(title = 'Justice by Pregnancy Health') + labs(x="Pregnancy Health", y="Justice") + theme_classic()
```

Gini (y) by Pregnancy Health (x)
```{r}
# With the regression line
ggplot(eardata, aes(x=PHI, y=Gini)) + geom_point()+
  geom_smooth(method=lm) + labs(title = 'Pregnancy Health by Inequality') + labs(title = 'Justice by Pregnancy Health') + labs(x="Pregnancy Health", y="Inequality") + theme_classic()
```

Gini (y) by Justice (x)
```{r}
# With the regression line
ggplot(eardata, aes(x=JusticeIndex, y=Gini)) + geom_point()+
  geom_smooth(method=lm) + labs(title = 'Justice by Inequality') + labs(title = 'Justice by Pregnancy Health') + labs(x="Justice", y="Inequality") + theme_classic()
```

Dependency Score (y) by Justice (x)
```{r}
# With the regression line
ggplot(eardata, aes(x=JusticeIndex, y=DependencyScore)) + geom_point()+
  geom_smooth(method=lm) + labs(title = 'Federal Dependency by Justice') + labs(title = 'Justice by Pregnancy Health') + labs(x="Justice", y="Dependency") + theme_classic()
```

Dependency Score (y) by Pregnancy Health (x)
```{r}
# With the regression line
ggplot(eardata, aes(x=PHI, y=DependencyScore)) + geom_point()+
  geom_smooth(method=lm) + labs(title = 'Federal Dependency by Pregnancy Health') + labs(title = 'Justice by Pregnancy Health') + labs(x="Pregancy Health", y="Dependency") + theme_classic()
```

Dependency Score (y) by Gini (x)
```{r}
# With the regression line
ggplot(eardata, aes(x=Gini, y=DependencyScore)) + geom_point()+
  geom_smooth(method=lm)+ labs(title = 'Federal Dependency by Inequality') + labs(title = 'Justice by Pregnancy Health') + labs(x="Ineqaulity", y="Dependency") + theme_classic()
```

